{
  "name": "@dofe/infra-rabbitmq",
  "version": "0.1.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "scripts": {
    "build": "echo 'source-referencing mode: skip tsc build'",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "peerDependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/config": "^4.0.0",
    "@nestjs/core": "^11.0.0",
    "amqp-connection-manager": "^5.0.0",
    "amqplib": "^0.10.0",
    "bullmq": "^5.0.0",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.8.0"
  },
  "dependencies": {
    "@dofe/infra-common": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
