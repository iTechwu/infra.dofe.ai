/**
 * i18n package
 *
 * This package contains JSON translation files used by nestjs-i18n.
 * The framework loads translations directly from the filesystem via the
 * `en/` and `zh-CN/` subdirectories, so no programmatic exports are needed.
 *
 * Supported locales: en, zh-CN
 */

export const i18nPath = __dirname;
