/**
 * DB Metrics 模块导出
 *
 * 注意: 此模块已扁平化 (2026-06-25)。
 * 旧路径 `db-metrics/src/*` 已移除，请直接使用 `@dofe/infra-prisma/db-metrics`。
 */
export { DbMetricsModule } from './db-metrics.module';
export * from './db-metrics.service';
