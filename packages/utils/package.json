{
  "name": "@dofe/infra-utils",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "peerDependencies": {
    "@repo/contracts": ">=0.0.0",
    "@repo/utils": ">=0.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
