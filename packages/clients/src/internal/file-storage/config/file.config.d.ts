declare const config: {
    qiniu: {
        bucketManager: string;
        uploadUrl: string;
        downloadUrl: string;
        objectManager: string;
        objectEnum: string;
        countSearch: string;
    };
};
export default config;
