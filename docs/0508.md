# @dofe/infra-* 包统一方案 — 深度分析与优化建议

> **日期**: 2026-05-08
> **状态**: Phase 1-6 已完成，Phase 7-9 规划中
> **目标**: 消除 models.dofe.ai / agents.dofe.ai / sso.dofe.ai 三方代码冗余

---

## 一、已完成的提取工作（Phase 1-6）

### Phase 1-4: SSO 客户端包 [@dofe/infra-clients/sso]

- `SsoMessageClient` — 服务间消息创建
- `SsoMessageProxyService` — 用户请求转发
- `SsoAuthClient` — 认证客户端（预留）

### Phase 5: JWKS 验证逻辑 [@dofe/infra-jwt]

- `JwksClient` — JWKS fetch + cache + JWK→PEM 转换
- agents/models AuthGuard 均已迁移使用

### Phase 6: OIDC 一致性修复

- `refreshToken()` 统一使用 OIDC discovery metadata
- `findOrCreateUserFromSso` 竞态条件重试顺序统一

---

## 二、剩余重复代码分析

> **Phase 7-8 已完成**: `@dofe/infra-contracts-base` 和 `@dofe/sso-browser` 已创建。

### 2A. contracts 基础模块重复 [DONE — Phase 7 完成]

**现状**: 所有三个项目各自维护相同的基础 contracts 定义：

| 包名 | agents.dofe.ai | models.dofe.ai | sso.dofe.ai | 重复度 |
|------|----------------|----------------|-------------|--------|
| `@repo/contracts/base` | ✅ | ✅ | ✅ | **100% 相同** |
| `@repo/contracts/errors` | ✅ | ✅ | ✅ | **100% 相同** |

> **注意**: `@repo/constants` **不纳入统一提取**。各项目的 `constants/api` 有大量自动生成内容，需保持各项目独立维护。各项目 `@repo/contracts` 只需 export `@dofe/infra-contracts-*` 即可。

**核心问题**: 
- `PaginationQuerySchema`、`PaginatedResponseSchema`、`ApiResponseSchema` 在三处各自定义
- `CommonErrorCode`、`AuthErrorCode`、`UserErrorCode` 在三处各自定义
- 每次更新一处需要同步更新其他两处（容易遗漏）

**已完成方案**:

```
infra.dofe.ai/packages/
├── contracts-base/  → @dofe/infra-contracts-base (v0.1.0) ✅ DONE
│   ├── PaginationQuerySchema, PaginatedResponseSchema, ApiResponseSchema
│   ├── withVersion(), getContractVersion(), CONTRACT_METADATA
│   └── EmptyQuerySchema, SuccessResponseSchema, ErrorResponseSchema
├── contracts/       → @dofe/infra-contracts (error-codes.ts 已存在)
│   └── error-codes.ts — CommonErrorCode, UserErrorCode, ApiErrorCode
└── sso-browser/     → @dofe/sso-browser (v0.1.0) ✅ DONE
```

**实施步骤**:
1. 创建 `@dofe/infra-contracts-base` 包，提取 `PaginationQuerySchema`、`PaginatedResponseSchema`
2. 创建 `@dofe/infra-contracts-errors` 包，提取统一错误码定义
3. 各项目 `@repo/contracts` 改为导入 `@dofe/infra-contracts-*` 并扩展项目特定部分

---

### 2B. 前端共享代码 [DONE — Phase 8 完成]

**完全相同的文件**:

| 文件 | agents | models | 行数 | 状态 |
|------|--------|--------|------|------|
| `apps/web/lib/sso-session.ts` | ✅ | ✅ | 118 | ✅ 已提取到 `@dofe/sso-browser` |

**已完成方案**:

```
infra.dofe.ai/packages/
├── sso-browser/     → @dofe/sso-browser (v0.1.0) ✅ DONE
│   ├── sso-session.ts   — checkSsoSession(config), checkSsoSessionViaIframe(config)
│   └── SsoSessionData, SsoUserInfo interfaces
```

**使用方式**:

```typescript
// agents.dofe.ai/apps/web/lib/sso-session.ts

import { checkSsoSession, checkSsoSessionViaIframe, type SsoSessionData } from '@dofe/sso-browser';

const SSO_BASE_URL = process.env.NEXT_PUBLIC_SSO_BASE_URL || 'https://sso.dofe.ai';

export async function checkSession() {
  return checkSsoSession({ ssoBaseUrl: SSO_BASE_URL });
}

export function checkSessionViaIframe() {
  return checkSsoSessionViaIframe({ ssoBaseUrl: SSO_BASE_URL });
}

---

### 2C. OIDC Client 服务 [MEDIUM PRIORITY]

**现状**: agents 和 models 各有 ~350 行 `OidcClientApiService`

**重复内容**:
- `openid-client` v6 discovery + authorization URL + PKCE
- Redis state/nonce/codeVerifier storage (600s TTL)
- `findOrCreateUserFromSso` 用户查找/创建流程
- Token refresh 代理

**项目特定部分** (无法共享):
- `SSO_CLIENT_ID` / `SSO_CLIENT_SECRET` 配置值
- `redirectUri` (agents: `/api/auth/oidc/callback`, models: `/auth/oidc/callback`)
- `UserInfoService` 本地用户查找 (依赖各项目的 Prisma schema)
- Controller (`@Public()` 装饰器位置、版本策略)

**建议方案**: 不提取到 infra，但可创建共享模板或接口定义

```typescript
// @dofe/infra-sso-client (可选)
interface OidcClientConfig {
  issuer: string;
  clientId: string;
  clientSecret: string;
  redirectUri: string;
  callbackFrontendUrl: string;
}

interface OidcClientInterface {
  getAuthorizationUrl(redirectUri?: string): Promise<{ url: string; state: string }>;
  handleCallback(code: string, state: string): Promise<{ redirectUrl: string }>;
  refreshToken(refreshToken: string): Promise<TokenResponse>;
  getLogoutUrl(idTokenHint?: string): string;
}
```

**已完成的优化**: Phase 6 已统一两边实现逻辑

---

### 2D. AuthGuard [MEDIUM PRIORITY]

**现状**: agents/models AuthGuard 仍有部分差异

| 特性 | agents | models | infra-common |
|------|--------|--------|--------------|
| JWKS 验证 | ✅ (via `@dofe/infra-jwt`) | ✅ (via `@dofe/infra-jwt`) | ✅ |
| 本地用户查找 | `UserInfoService.get({ ssoSub })` | `UserInfoService.get({ ssoSub })` | N/A (domain) |
| `@Public()` 装饰器 | class-level | method-level | method-level |
| 内部服务绕过 | `isInternalService` flag | 无 | 无 |
| 匿名路径限制 | `outOfAnonymityPathConfig` | 无 | 无 |

**建议方案**: 不完全提取（依赖 domain 层的 UserInfoService），但可标准化接口

```typescript
// @dofe/infra-auth-guard (可选)
interface AuthGuardDependencies {
  jwtService: JwtService;
  jwksClient: JwksClient;
  userLookup: (ssoSub: string) => Promise<LocalUser | null>;
  reflector: Reflector;
}
```

---

### 2E. SSO Internal API 客户端 [HIGH PRIORITY]

**现状**: SSO 的 `internalContract` 提供了类型化的内部 API，但 models/agents 未使用

**可用端点**:
| 端点 | 功能 | 当前状态 |
|------|------|---------|
| `POST /internal/verify-token` | 服务端验证任意 token | 未使用 (本地 JWKS 验证) |
| `GET /internal/users/:id` | 获取用户信息 | 未使用 |
| `POST /internal/users/batch` | 批量获取用户 | 未使用 |
| `GET /internal/tenants/:id` | 获取租户信息 | 未使用 |
| `POST /internal/messages` | 创建系统消息 | ✅ `SsoMessageClient` |
| `GET /internal/users/:id/sessions` | 获取用户会话列表 | 未使用 |
| `POST /internal/users/:id/sessions/:clientId/revoke` | 撤销特定会话 | 未使用 |
| `POST /internal/users/:id/sessions/revoke-all` | 撤销所有会话 | 未使用 |

**建议方案**: 扩展 `@dofe/infra-clients/sso` 的 `SsoAuthClient`

```typescript
// sso-auth.client.ts 扩展
class SsoAuthClient {
  // 已有
  verifyToken(accessToken): Promise<{ valid, userId?, expiresAt? }>;
  getSession(cookieHeader?): Promise<unknown>;
  getUser(userId): Promise<unknown>;
  getJwks(): Promise<{ keys: Array<Record<string, unknown>> }>;
  
  // 新增
  batchGetUsers(userIds: string[]): Promise<UserInfo[]>;
  getTenant(tenantId: string): Promise<TenantInfo>;
  getUserSessions(userId: string): Promise<Session[]>;
  revokeSession(userId: string, clientId: string): Promise<void>;
  revokeAllSessions(userId: string): Promise<void>;
}
```

---

### 2F. 其他可提取内容 [LOW PRIORITY]

| 内容 | 位置 | 建议 |
|------|------|------|
| `@repo/ui` 组件包 | 各项目 packages/ui | 已有共享模式，可集中到 infra |
| Prisma DB 服务生成脚本 | 各项目 scripts/generate-db-crud.js | 可统一为 `@dofe/infra-prisma-generator` |
| Next.js proxy.ts | 各项目 apps/web/proxy.ts | 模式相似，可提取为模板 |
| Winston logger 配置 | 各项目 libs/infra/common/winston | 已有 `@dofe/infra-common` |

---

## 三、优化方案总览

### Tier 1 — 高优先级（已完成）

| Phase | 目标 | 状态 | 版本 |
|-------|------|------|------|
| **Phase 7** | 创建 `@dofe/infra-contracts-base` | ✅ DONE | v0.1.0 |
| **Phase 8** | 创建 `@dofe/sso-browser` | ✅ DONE | v0.1.0 |

### Tier 2 — 中优先级（已完成）

| Phase | 目标 | 状态 | 说明 |
|-------|------|------|------|
| **Phase 9** | 扩展 `SsoAuthClient` 实现全部 internalContract 方法 | ✅ DONE | 启用新跨服务模式 |

### 不纳入统一提取的内容

| 内容 | 原因 |
|------|------|
| `@repo/constants` | 各项目 `constants/api` 有自动生成内容，需保持独立 |
| `@repo/validators` | 各项目有不同业务验证需求 |
| `@repo/types` | 各项目有不同类型定义 |
| OIDC Client 实现 | 依赖 Redis + UserInfoService（项目特定） |
| AuthGuard 实现 | 依赖 UserInfoService（项目特定） |

---

## 四、Phase 7 详细计划 — @dofe/infra-contracts-base + errors

### 4A. @dofe/infra-contracts-base

```typescript
// infra.dofe.ai/packages/contracts-base/src/base.ts

import { z } from 'zod';

// 分页查询 Schema
export const PaginationQuerySchema = z.object({
  limit: z.coerce.number().positive().optional().default(20),
  page: z.coerce.number().positive().min(1).optional().default(1),
  sort: z.string().optional(),
  asc: z.enum(['asc', 'desc']).optional(),
});

// 分页响应 Schema 工厂
export const PaginatedResponseSchema = <T extends z.ZodTypeAny>(itemSchema: T) =>
  z.object({
    list: z.array(itemSchema),
    total: z.number(),
    page: z.number(),
    limit: z.number(),
  });

// API 响应包装 Schema
export const ApiResponseSchema = <T extends z.ZodTypeAny>(dataSchema: T) =>
  z.object({
    code: z.number(),
    msg: z.string().optional(),
    data: dataSchema,
  });

export const createApiResponse = ApiResponseSchema;

// 版本兼容包装
export function withVersion<T extends z.ZodTypeAny>(
  schema: T,
  version: string,
): T {
  return schema; // 可扩展添加 version 字段
}
```

### 4B. @dofe/infra-contracts-errors

```typescript
// infra.dofe.ai/packages/contracts-errors/src/codes.ts

export enum CommonErrorCode {
  Success = 0,
  BadRequest = 400,
  UnAuthorized = 401,
  Forbidden = 403,
  NotFound = 404,
  Conflict = 409,
  InternalServerError = 500,
  ServiceUnavailable = 503,
  SignatureError = 4011,
  // ...
}

export enum AuthErrorCode {
  InvalidCredentials = 1001,
  TokenExpired = 1002,
  SessionNotFound = 1003,
  // ...
}

export enum UserErrorCode {
  UserNotFound = 2001,
  UserAlreadyExists = 2002,
  // ...
}

// 错误消息映射
export const ErrorMessages: Record<number, string> = {
  [CommonErrorCode.UnAuthorized]: '未授权访问',
  [CommonErrorCode.NotFound]: '资源不存在',
  // ...
};
```

---

## 五、Phase 8 详细计划 — @dofe/sso-browser

### 5A. 目标范围

提取前端跨子域 session 检测代码：

```typescript
// infra.dofe.ai/packages/sso-browser/src/sso-session.ts

export interface SsoSessionData {
  user: UserInfo;
  access: string;
  refresh: string;
  accessExpire: number;
  expire: number;
}

export interface SsoSessionConfig {
  ssoBaseUrl: string;
  timeoutMs?: number;
}

export function checkSsoSession(config: SsoSessionConfig): Promise<SsoSessionData | null>;
export function checkSsoSessionViaIframe(config: SsoSessionConfig): Promise<SsoSessionData | null>;
```

### 5B. 使用方式

```typescript
// agents.dofe.ai/apps/web/lib/sso-session.ts

import { checkSsoSession, checkSsoSessionViaIframe } from '@dofe/sso-browser';

const SSO_BASE_URL = process.env.NEXT_PUBLIC_SSO_BASE_URL || 'https://sso.dofe.ai';

export async function checkSession() {
  return checkSsoSession({ ssoBaseUrl: SSO_BASE_URL });
}

export function checkSessionViaIframe() {
  return checkSsoSessionViaIframe({ ssoBaseUrl: SSO_BASE_URL });
}
```

---

## 六、架构演进图

```
                    ┌──────────────────────────────┐
                    │       infra.dofe.ai           │
                    │  ┌──────────────────────────┐ │
                    │  │ @dofe/infra-contracts-base│ │
                    │  │ @dofe/infra-contracts-err │ │
                    │  │ @dofe/infra-jwt           │ │
                    │  │ @dofe/infra-clients/sso   │ │
                    │  │ @dofe/sso-browser         │ │
                    │  └──────────────────────────┘ │
                    └──────┬───────────┬────────────┘
                           │           │
         npm packages      │           │
                           │           │
          ┌────────────────┘           └────────────────┐
          │                                             │
          ▼                                             ▼
┌─────────────────────┐                    ┌─────────────────────┐
│   models.dofe.ai    │                    │   agents.dofe.ai    │
│                     │                    │                     │
│ @repo/contracts     │                    │ @repo/contracts     │
│   ├─ import from    │                    │   ├─ import from    │
│   │  @dofe/infra-*  │                    │   │  @dofe/infra-*  │
│   └─ project-       │                    │   └─ project-       │
│     specific        │                    │     specific        │
│                     │                    │                     │
│ apps/web/lib/       │                    │ apps/web/lib/       │
│   ├─ sso-session.ts │                    │   ├─ sso-session.ts │
│   │  (import @dofe) │                    │   │  (import @dofe) │
│   └─ ...            │                    │   └─ ...            │
└─────────────────────┘                    └─────────────────────┘
```

---

## 七、验证清单

| 验证项 | 当前状态 | Phase 7 后 | Phase 8 后 |
|--------|----------|-----------|-----------|
| API schema 重复 | 三方各自维护 | ✅ 统一 (`@dofe/infra-contracts-base`) | ✅ |
| Error codes 重复 | 三方各自维护 | ✅ 统一 (`@dofe/infra-contracts/error-codes`) | ✅ |
| 前端 sso-session.ts | 两方完全相同 | ⏳ | ✅ 统一 (`@dofe/sso-browser`) |
| JWKS 验证逻辑 | ✅ Phase 5 | ✅ | ✅ |
| OIDC 一致性 | ✅ Phase 6 | ✅ | ✅ |

## 八、Phase 7-8 实施记录

### Phase 7: @dofe/infra-contracts-base

**新建文件**:
- `packages/contracts-base/src/base.ts` — Zod schemas + API versioning
- `packages/contracts-base/src/index.ts` — Barrel export
- `packages/contracts-base/package.json` — v0.1.0, CommonJS
- `packages/contracts-base/tsconfig.json` — Node16 module resolution

**关键内容**:
- `PaginationQuerySchema`, `PaginatedResponseSchema`, `ApiResponseSchema`
- `ExtendedPaginatedResponseSchema`, `EmptyQuerySchema`
- `withVersion()`, `getContractVersion()`, `CONTRACT_METADATA`
- `SuccessResponseSchema`, `ErrorResponseSchema`, `EnhancedErrorResponseSchema`

**验证**: `pnpm build` 通过，dist 生成 5 files。

### Phase 8: @dofe/sso-browser

**新建文件**:
- `packages/sso-browser/src/sso-session.ts` — Cross-subdomain session detection
- `packages/sso-browser/src/index.ts` — Barrel export
- `packages/sso-browser/package.json` — v0.1.0, CommonJS, zero dependencies
- `packages/sso-browser/tsconfig.json` — Node16 module resolution

**关键内容**:
- `checkSsoSession(config)` — Layer 1: fetch API to `/auth/session`
- `checkSsoSessionViaIframe(config)` — Layer 2: iframe + postMessage
- `checkSsoSessionCombined(config)` — Combined fallback
- `SsoSessionData`, `SsoUserInfo`, `SsoSessionConfig` interfaces

**验证**: `pnpm build` 通过，dist 生成 5 files。

---

## 九、后续跟踪

1. **Phase 7**: 创建 `@dofe/infra-contracts-base` + `@dofe/infra-contracts-errors` — 预估工作量 2h
2. **Phase 8**: 创建 `@dofe/sso-browser` — 预估工作量 1h
3. **Phase 9**: 扩展 `SsoAuthClient` — 预估工作量 2h

**总计**: Phase 7-9 已完成 (~5h)，各项目集成已完成。

---

## 十一、各项目集成状态

### agents.dofe.ai

| 组件 | 状态 | 说明 |
|------|------|------|
| `@dofe/sso-browser` | ✅ 已集成 | `apps/web/package.json` 已添加依赖 |
| `apps/web/lib/sso-session.ts` | ✅ 已简化 | 改为导入 `@dofe/sso-browser` + 类型适配 |
| `@dofe/infra-clients/sso` | ✅ 已使用 | `SsoMessageProxyService`, `SsoMessageClient` |
| type-check | ✅ PASS | `pnpm type-check` 通过 |

### models.dofe.ai

| 组件 | 状态 | 说明 |
|------|------|------|
| `@dofe/sso-browser` | ✅ 已集成 | `apps/web/package.json` 已添加依赖 |
| `apps/web/lib/sso-session.ts` | ✅ 已简化 | 改为导入 `@dofe/sso-browser` + 类型适配 |
| `@dofe/infra-clients/sso` | ✅ 已使用 | `SsoMessageProxyService` |
| type-check | ✅ PASS | `pnpm type-check` 通过 |

### sso.dofe.ai

| 组件 | 状态 | 说明 |
|------|------|------|
| `@dofe/infra-contracts-base` | ⏳ 可选 | 内部 `base.ts` 与 infra 版本高度相似，可考虑迁移 |
| `@dofe/sso-browser` | ❌ 不需要 | SSO 是服务端，不需要前端 session 检测 |
| `@dofe/infra-clients/sso` | ❌ 不需要 | SSO 是服务端，不需要调用自己 |

**注意**: sso.dofe.ai 的 `@repo/contracts/base.ts` 与 `@dofe/infra-contracts-base` 高度相似（95%），可在后续 PR 中迁移，但当前不影响功能。

---

## 十二、使用示例

### 前端 SSO Session 检测

```typescript
// apps/web/lib/sso-session.ts
import { checkSsoSession, checkSsoSessionViaIframe } from '@dofe/sso-browser';
import type { LoginSuccess } from '@repo/contracts';

const SSO_BASE_URL = process.env.NEXT_PUBLIC_SSO_BASE_URL || 'https://sso.dofe.ai';

export async function checkSession(): Promise<LoginSuccess | null> {
  const data = await checkSsoSession({ ssoBaseUrl: SSO_BASE_URL });
  return data ? adaptToLoginSuccess(data) : null;
}
```

### 后端 SsoAuthClient 使用

```typescript
// apps/api/src/modules/user/user.service.ts
import { SsoAuthClient } from '@dofe/infra-clients/sso';

@Injectable()
export class UserService {
  constructor(private readonly ssoAuthClient: SsoAuthClient) {}

  async getUserFromSso(userId: string) {
    return this.ssoAuthClient.getUser(userId);
  }

  async batchGetUsers(userIds: string[]) {
    return this.ssoAuthClient.batchGetUsers(userIds);
  }

  async revokeUserSessions(userId: string) {
    return this.ssoAuthClient.revokeAllSessions(userId);
  }
}
```

---

## Phase 9 实施记录

### Phase 9: SsoAuthClient 扩展

**修改文件**:
- `packages/clients/src/internal/sso/sso-auth.client.ts` — 扩展全部 internalContract 方法

**新增方法**:

| 方法 | 功能 | API 端点 |
|------|------|----------|
| `verifyToken(token)` | 验证 token 有效性 | POST `/internal/verify-token` |
| `getUser(userId)` | 按 ID 获取用户信息 | GET `/internal/users/:id` |
| `batchGetUsers(userIds)` | 批量获取用户信息（最多 100） | POST `/internal/users/batch` |
| `getTenant(tenantId)` | 按 ID 获取租户信息 | GET `/internal/tenants/:id` |
| `getSession(cookieHeader?)` | 检查会话状态（跨子域） | GET `/auth/session` |
| `getUserSessions(userId)` | 获取用户活跃会话列表 | GET `/internal/users/:id/sessions` |
| `revokeSession(userId, clientId)` | 撤销特定 OIDC 会话 | POST `/internal/users/:id/sessions/:clientId/revoke` |
| `revokeAllSessions(userId)` | 撤销所有会话 | POST `/internal/users/:id/sessions/revoke-all` |
| `getKeyStatus()` | 获取密钥状态 | GET `/internal/keys/status` |
| `rotateKeys()` | 轮换密钥 | POST `/internal/keys/rotate` |
| `purgeKeys()` | 清理过期密钥 | POST `/internal/keys/purge` |
| `getJwks()` | 获取 JWKS 公钥集合 | GET `/.well-known/jwks.json` |

**配置强制验证**:

在 `onModuleInit()` 中强制检查以下环境变量，缺失时抛出错误：

| 环境变量 | 说明 | 错误提示 |
|----------|------|----------|
| `SSO_INTERNAL_API_URL` | SSO 内部 API 地址 | 必须配置 |
| `SSO_API_URL` | SSO 公开 API 地址 | 必须配置 |
| `INTERNAL_API_SECRET` | 服务间认证密钥 | 必须配置 |
| `SSO_SERVICE_NAME` | 服务标识（如 `models.dofe.ai`） | 必须配置 |

**新增导出类型**:

- `SsoInternalUser`, `SsoInternalTenant`
- `SsoOidcSessionInfo`, `SsoMainSessionInfo`, `SsoUserSessionsResponse`
- `SsoKeyInfo`, `SsoKeyStatusResponse`, `SsoKeyRotateResponse`, `SsoKeyPurgeResponse`

**验证**: `pnpm build` 通过，105 exports。

---

## 附录：当前三方共享包对比

### packages/ 对比

| 目录 | agents.dofe.ai | models.dofe.ai | sso.dofe.ai | 统一建议 |
|------|----------------|----------------|-------------|---------|
| config | ✅ | ✅ | ✅ | 保持各项目独立 (Tailwind 配置) |
| constants | ✅ | ✅ | ✅ | **不提取** — 各项目有自动生成内容 |
| contracts | ✅ | ✅ | ✅ | **base + errors 提取到 infra** |
| types | ✅ | ✅ | ✅ | 保持各项目独立 |
| ui | ✅ | ✅ | ✅ | 保持各项目独立 (不同 UI 需求) |
| utils | ✅ | ✅ | ✅ | 大部分已在 @dofe/infra-utils |
| validators | ✅ | ✅ | ✅ | 保持各项目独立 |

### contracts/api/ 对比

| Contract | agents | models | sso | 说明 |
|----------|--------|--------|-----|------|
| analytics.contract.ts | ✅ | ✅ | ✅ | 相似结构，可统一 base |
| audit-log.contract.ts | ✅ | ✅ | ✅ | 相似结构 |
| download.contract.ts | ✅ | ✅ | ✅ | 完全相同 |
| message.contract.ts | ✅ | ✅ | ✅ | 相似结构 |
| internal.contract.ts | ❌ | ❌ | ✅ | **SSO only — 应提供 client** |
| auth.contract.ts | ❌ | ❌ | ✅ | SSO only |
| oidc.contract.ts | ❌ | ❌ | ✅ | SSO only |
| user.contract.ts | ❌ | ✅ | ✅ | 部分重叠 |